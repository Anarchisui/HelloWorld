#include "stdafx.h"
#include "iostream"
using namespace std;

int main()
{
	int  test[];// you can leave [] empty only if its a char array and only if you're hardcoding that char array 
	char test1[];//using the = sign and not if you want to get input aferwards by using cin.getline or anything else 
	char test2[]="";//because in that case these member functions require index of array which has to be defined prevoiusly
	cout << test;

	system("pause");
	return 0;
}
